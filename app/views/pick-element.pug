.pick-element
    h2.sub-title {{observation.school}}
    h3.sub-sub-title {{observationType}}
    h3.sub-sub-title {{observation.grade}}
    h3.sub-sub-title {{observation.teacher}}
    h1.title What Element?
    .buttons-group
        button(ng-repeat="element in data.elements", ng-click="pickElement(element)", ng-style='{"background-color": element.color}') {{element.name}}

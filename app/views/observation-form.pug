h2.sub-title {{observation.school}}
h3.sub-sub-title {{observation.observationType}}
h3.sub-sub-title {{observation.grade}}
h3.sub-sub-title {{observation.teacher}}
h3.sub-sub-title {{observation.subject}}
h3.sub-sub-title {{observation.observationKind}} Observation

form(ng-submit='goToClustersObservedView()')
  .form-group(ng-if='observation.observationKind === "Crew"')
    p.title Crew Name
    input(type='text' placeholder='Crew Name' ng-model='form.crewName' required)
  .form-group(ng-if='observation.observationKind === "Lesson"')
    p.title Context
    p.label What? Number of Peope? Time Spent? etc.
    textarea(name="", cols="50", rows="10" required ng-model='form.context')
  .form-group
    p.title Content
    p.sub-title(ng-if='observation.observationKind === "Lesson"') Level and Complexity
    p.label(ng-if='observation.observationKind === "Crew"') What's the crew working on? Is there a theme/goal/focus?
    p.label(ng-if='observation.observationKind === "Lesson"') What is the task at hand? What are the targets and standards?
    textarea(name="", cols="50", rows="10" ng-model='form.content' required)
  .form-group
    p.title Teacher
    p.sub-title Knowledge and Skill
    p.label What is the teacher doing? What is the teacher saying?
    textarea(name="", cols="50", rows="10" ng-model='form.teacher' required)
  .form-group
    p.title Student
    p.sub-title(ng-if='observation.observationKind === "Crew"') Role in Crew
    p.sub-title(ng-if='observation.observationKind === "Lesson"') Role in Instruction
    p.label What are the students doing? What are the students saying?
    textarea(name="", cols="50", rows="10" ng-model='form.student' required)
  .form-group
    p.title Conclusions
    p.label Best when... Better if...
    textarea(name="", cols="50", rows="10" ng-model='form.conclusions' required)
  input(type='submit' value='SUBMIT')